---
import LifecycleReport from "./LifecycleReport.astro";
import { lifecycleStages } from "../data/lifecycle-stages";
import { u } from "../lib/url";

const stages = lifecycleStages;

---

<section id="lifecycle" class="border-y border-[var(--rule)] bg-[var(--bg)] scroll-mt-24">
  <div class="mx-auto max-w-[1180px] px-6 md:px-10 py-12">
    <div class="max-w-[720px] mx-auto text-center mb-6">
      <p class="eyebrow mb-2 reveal">The lifecycle</p>
      <h2 class="font-semibold text-[1.75rem] md:text-[2rem] leading-[1.1] tracking-[-0.015em] reveal">
        A diverse toolset for everything actuaries do
      </h2>
      <p class="reveal text-[0.9375rem] leading-[1.55] text-[var(--fg-muted)] mt-3" data-reveal-delay="120">
        Actuaries do many different things. One language, reproducible across every stage.
      </p>
    </div>

    {/* Tab strip */}
    <div class="lifecycle-tabs reveal" role="tablist" aria-label="Carrier product lifecycle">
      {stages.map((s, i) => (
        <button
          type="button"
          role="tab"
          id={`lc-tab-${s.id}`}
          aria-controls={`lc-panel-${s.id}`}
          aria-selected={i === 0 ? "true" : "false"}
          data-stage={s.id}
          class:list={["lifecycle-tab", i === 0 && "is-active"]}
        >
          <span class="lifecycle-tab__num">{String(i + 1).padStart(2, "0")}</span>
          <span class="lifecycle-tab__label">{s.label}</span>
        </button>
      ))}
    </div>

    {/* Panels */}
    <div class="mt-5 reveal" data-reveal-delay="120">
      {stages.map((s, i) => (
        <div
          role="tabpanel"
          id={`lc-panel-${s.id}`}
          aria-labelledby={`lc-tab-${s.id}`}
          data-stage-panel={s.id}
          class:list={["lifecycle-panel", i !== 0 && "is-hidden"]}
        >
          <div class="grid md:grid-cols-12 gap-6 md:gap-8 items-start">
            <div class="md:col-span-5">
              <h3 class="font-semibold text-[1.25rem] md:text-[1.375rem] leading-[1.2] tracking-[-0.008em]">
                {s.title}
              </h3>
              <p class="mt-3 text-[0.9375rem] leading-[1.55] text-[var(--fg-muted)]">
                {s.body}
              </p>
              <p class="lifecycle-followup__eyebrow mt-5">Ask a follow-up</p>
              <ul class="lifecycle-followup__list">
                {s.followups.slice(0, 3).map((f) => (
                  <li class="lifecycle-followup">
                    <p class="lifecycle-followup__title">{f.title}</p>
                    <p class="lifecycle-followup__body">{f.body}</p>
                  </li>
                ))}
              </ul>
            </div>
            <div class="md:col-span-7">
              <figure class="lifecycle-mockup">
                <LifecycleReport report={s.report} />
              </figure>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .lifecycle-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    border-bottom: 1px solid var(--rule);
    padding-bottom: 12px;
  }
  .lifecycle-tab {
    --lc-accent: #B400FE;
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 8px 14px;
    border-radius: 999px;
    background: transparent;
    border: 1px solid var(--rule);
    color: var(--fg);
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: -0.005em;
    cursor: pointer;
    transition: background 180ms ease, border-color 180ms ease, color 180ms ease;
  }
  .lifecycle-tab:hover {
    border-color: color-mix(in oklab, var(--lc-accent) 50%, transparent);
    color: var(--lc-accent);
  }
  .lifecycle-tab.is-active {
    background: color-mix(in oklab, var(--lc-accent) 8%, transparent);
    border-color: color-mix(in oklab, var(--lc-accent) 55%, transparent);
    color: var(--lc-accent);
  }
  .lifecycle-tab__num {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    letter-spacing: 0.14em;
    color: color-mix(in oklab, var(--lc-accent) 80%, var(--fg-muted));
    font-weight: 600;
  }
  .lifecycle-tab.is-active .lifecycle-tab__num {
    color: var(--lc-accent);
  }

  .lifecycle-panel.is-hidden {
    display: none;
  }

  .lifecycle-followup__eyebrow {
    font-family: var(--font-mono);
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: color-mix(in oklab, #B400FE 70%, var(--fg-muted));
    margin-bottom: 12px;
  }
  .lifecycle-followup__list {
    display: grid;
    grid-template-columns: 1fr;
    gap: 10px;
  }

  .lifecycle-mockup {
    --lc-accent: #B400FE;
    margin: 0;
    border-radius: 5px;
    overflow: hidden;
    background: #ffffff;
    border: 1px solid color-mix(in oklab, var(--lc-accent) 18%, rgba(14, 14, 12, 0.08));
    box-shadow:
      0 1px 0 rgba(14, 14, 12, 0.04),
      0 24px 60px -34px color-mix(in oklab, var(--lc-accent) 40%, rgba(14, 14, 12, 0.18));
  }
  .lifecycle-followup {
    --lc-accent: #B400FE;
    border: 1px solid color-mix(in oklab, var(--lc-accent) 14%, rgba(14, 14, 12, 0.08));
    background: #ffffff;
    border-radius: 5px;
    padding: 9px 12px;
    color: var(--color-ink, #0e0e0c);
    box-shadow:
      0 1px 0 rgba(14, 14, 12, 0.02),
      0 10px 28px -24px color-mix(in oklab, var(--lc-accent) 36%, rgba(14, 14, 12, 0.18));
  }
  :global([data-theme="dark"]) .lifecycle-followup {
    background: rgba(250, 247, 240, 0.03);
    border-color: color-mix(in oklab, var(--lc-accent) 24%, rgba(250, 247, 240, 0.12));
    color: var(--color-bone);
  }
  .lifecycle-followup__title {
    font-size: 0.875rem;
    font-weight: 600;
    line-height: 1.25;
    letter-spacing: -0.005em;
    color: var(--color-ink, #0e0e0c);
  }
  :global([data-theme="dark"]) .lifecycle-followup__title {
    color: var(--color-bone);
  }
  .lifecycle-followup__body {
    margin-top: 3px;
    font-size: 0.78125rem;
    line-height: 1.45;
    color: var(--color-smoke, #6b6760);
  }
  :global([data-theme="dark"]) .lifecycle-followup__body {
    color: rgba(250, 247, 240, 0.72);
  }
</style>

<script is:inline>
  (function () {
    const tabs = document.querySelectorAll(".lifecycle-tab");
    const panels = document.querySelectorAll("[data-stage-panel]");
    if (!tabs.length) return;
    const validStages = new Set(Array.from(tabs).map(function (t) { return t.getAttribute("data-stage"); }));
    function selectStage(stage) {
      if (!validStages.has(stage)) return false;
      tabs.forEach(function (x) {
        const active = x.getAttribute("data-stage") === stage;
        x.classList.toggle("is-active", active);
        x.setAttribute("aria-selected", active ? "true" : "false");
      });
      panels.forEach(function (p) {
        p.classList.toggle("is-hidden", p.getAttribute("data-stage-panel") !== stage);
      });
      return true;
    }
    tabs.forEach(function (t) {
      t.addEventListener("click", function () { selectStage(t.getAttribute("data-stage")); });
    });
    function syncFromHash() {
      const hash = (window.location.hash || "").replace(/^#/, "");
      if (selectStage(hash)) {
        const section = document.getElementById("lifecycle");
        if (section) section.scrollIntoView({ behavior: "smooth", block: "start" });
      }
    }
    syncFromHash();
    window.addEventListener("hashchange", syncFromHash);
  })();
</script>
